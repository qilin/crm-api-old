- args:
    name: user
    table:
      name: user_group
      schema: public
    using:
      manual_configuration:
        column_mapping:
          user_id: id
        remote_table:
          name: users
          schema: public
  type: create_array_relationship
