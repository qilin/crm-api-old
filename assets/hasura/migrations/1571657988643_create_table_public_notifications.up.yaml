- args:
    sql: CREATE TABLE "public"."notifications"("id" serial NOT NULL, "text" text NOT
      NULL, "timestamp" timestamptz NOT NULL DEFAULT now(), "viewed" boolean NOT NULL
      DEFAULT false, "user_id" integer NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY
      ("user_id") REFERENCES "public"."users"("id") ON UPDATE restrict ON DELETE restrict,
      UNIQUE ("id"));
  type: run_sql
- args:
    name: notifications
    schema: public
  type: add_existing_table_or_view
